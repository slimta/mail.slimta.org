PYMAP_ARGS=--debug --systemd-sockets \
	--cert /etc/ssl/certs/local/fullchain.pem --key /etc/ssl/certs/local/privkey.pem \
	--set-uid slimta --set-gid mail \
	redis --users-key 'slimta:address:{name}' --users-json --select 1
